{
  "name": "e2e-playwright-pom-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:debug": "PWDEBUG=1 playwright test --debug",
    "install:drivers": "playwright install --with-deps",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^20.19.22",
    "typescript": "^5.4.2"
  }
}
